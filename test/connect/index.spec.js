import rootReducer from '../__mocks__/rootReducer';
import rootSaga from '../__mocks__/rootSaga';
// import connect from 'connect';

describe('connect', () => {
  it('should have a connect', () => {
    // const connectV = connect();
    expect(1 + 2).toBe(3);
  });
});

